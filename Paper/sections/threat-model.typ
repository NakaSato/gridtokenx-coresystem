#heading(level: 1)[SYSTEM MODEL AND THREAT MODEL] <sec:threat-model>

ส่วนนี้กำหนดแบบจำลองระบบ (system model) ผู้มีส่วนร่วม (actors) สมมติฐานความเชื่อถือ (trust assumptions) และแบบจำลองผู้โจมตี (adversary model) ของระบบจำลอง เพื่อให้ขอบเขตด้านความปลอดภัยของสถาปัตยกรรมแบบแยกชั้นชัดเจนก่อนการอภิปรายแบบจำลองการชำระธุรกรรมใน @sec:settlement-model-invariants ทั้งนี้ขอบเขตของงานเป็นการออกแบบบนระบบจำลอง การวิเคราะห์ด้านล่างจึงระบุทั้งภัยคุกคามที่กลไกปัจจุบันรองรับและความเชื่อถือที่ยังคงเหลืออยู่ (residual trust) อย่างตรงไปตรงมา

#heading(level: 2)[System Model and Actors]
ระบบประกอบด้วยผู้มีส่วนร่วมหลักดังนี้
- Smart Meter (edge device): อุปกรณ์ปลายทางที่ถือกุญแจคู่ Ed25519 ประจำเครื่อง ลงนามค่าอ่านพลังงานก่อนส่งออก
- Aggregator Bridge: ชั้นตรวจสอบและรวบรวมข้อมูลนอกเชน ตรวจลายเซ็น Ed25519 ของทุกค่าอ่านเทียบกับกุญแจสาธารณะของอุปกรณ์ ถอดรหัส payload ตามมาตรฐาน DLMS/COSEM และประเมินเงื่อนไขปริมาณพลังงานคงเหลือและ Grid stability
- Trading Service: จับคู่คำสั่งซื้อขายด้วย Continuous Double Auction (CDA)
- Chain Bridge: ทำหน้าที่เป็น Reference Monitor และเป็นบริการเดียวที่ถือกุญแจลงนามและเรียก Solana RPC โดยตรง ทุกธุรกรรมที่เขียนลงเชนถูกบีบให้ผ่านเส้นทางลงนามเดียว (single signing path)
- Anchor Programs: ชั้นบังคับใช้กติกาบนเชนที่รับเฉพาะคู่คำสั่งที่ผ่านการตรวจสอบแล้ว และทำหน้าที่เป็นชั้น settlement และ audit
- PoA / Governance Authority: หน่วยงานผู้มีสิทธิ์หลักตามนโยบาย consortium ที่ควบคุมการรับรอง REC สิทธิ์ผู้มีอำนาจ และ allow-list ของ aggregator

#heading(level: 2)[Trust Assumptions]
สมมติฐานความเชื่อถือของระบบถูกบังคับใช้ในสามขอบเขต ได้แก่ ขอบเขตอุปกรณ์-สู่-บริดจ์ (device-to-bridge) ขอบเขตระหว่างบริการ (service-to-service) และขอบเขตบริการ-สู่-เชน (service-to-chain)

ในขอบเขตอุปกรณ์-สู่-บริดจ์ ตัวตนของอุปกรณ์แต่ละเครื่องผูกกับกุญแจสาธารณะ Ed25519 ที่ลงทะเบียนไว้ Aggregator Bridge ตรวจลายเซ็นของค่าอ่านทุกรายการ (ลายเซ็น 64 ไบต์ กุญแจสาธารณะ 32 ไบต์) ทั้งแบบรายจุดและแบบกลุ่ม (batch) โดยใช้นโยบายแบบ fail-closed กล่าวคือเมื่อค้นกุญแจไม่พบหรือแหล่งเก็บกุญแจไม่ตอบสนอง ระบบจะปฏิเสธ (error) มิใช่ยอมรับค่าอ่านโดยปริยาย ส่วน payload แบบ Binary ตามมาตรฐาน DLMS/COSEM ถูกเข้ารหัสด้วย AES-256-GCM ด้วยกุญแจประจำอุปกรณ์ ในโหมด production หากอุปกรณ์ไม่มีกุญแจเข้ารหัส เฟรมนั้นจะถูกข้าม (fail-closed) และเส้นทาง plaintext เปิดใช้ได้เฉพาะในโหมดพัฒนาเท่านั้นผ่านการตั้งค่าที่ระบุชัด

ในขอบเขตระหว่างบริการที่เขียนสู่เชน (โดยเฉพาะเส้นทางสู่ Chain Bridge) การสื่อสารใช้ ConnectRPC บน mutually authenticated TLS (mTLS) โดยผูกตัวตนของบริการด้วย SPIFFE @spiffe2018 X.509 identity ที่ตรวจสอบจากใบรับรองฝั่ง client ในขั้นตอน handshake สิทธิ์การเข้าถึงถูกกำหนดจาก SPIFFE URI ของใบรับรองที่ผ่านการตรวจสอบแล้ว แปลงเป็นบทบาทบริการ (service role) เช่น `AggregatorBridge`, `TradingMatcher`, `SettlementService` ทั้งนี้ตัวตนถูกพิจารณาจากชั้น transport (L4) มิใช่ส่วนหัวของแอปพลิเคชัน (L7) ผู้เรียกที่ไม่ผ่านการตรวจสอบจะได้รับบทบาท `Unknown` อนึ่ง เส้นทางรับข้อมูลมาตรวัดที่ Aggregator Bridge ใช้กลไกพิสูจน์ตัวตนระดับคำขอด้วย API key (ตรวจสอบผ่าน IAM พร้อม static key สำรอง) ประกอบกับลายเซ็น Ed25519 รายค่าอ่าน มิใช่ mTLS/SPIFFE ส่วนการยกเว้นตรวจสอบ (insecure mode) ที่ให้สิทธิ์เต็มมีไว้สำหรับโหมดพัฒนาเท่านั้น

ในขอบเขตบริการ-สู่-เชน Chain Bridge เป็นบริการเดียวที่ถือกุญแจและส่งธุรกรรม โดยกุญแจลงนามถูกจัดเก็บใน HashiCorp Vault Transit (Ed25519) และไม่เคยเข้าสู่หน่วยความจำของกระบวนการ การลงนามถูกจำกัดด้วยชื่อกุญแจที่ได้รับอนุญาตเท่านั้น สำหรับเส้นทางเขียนแบบอะซิงโครนัสผ่าน NATS JetStream @natsio2024 ผู้เผยแพร่ (publisher) ต้องลงนามซองข้อความ (envelope) ด้วยลายเซ็น ECDSA P-256 เหนือไบต์แบบ canonical ของซอง ซึ่งตรวจสอบเทียบกับกุญแจสาธารณะในใบรับรอง client ของผู้เผยแพร่ และ Chain Bridge ตรวจสอบลำดับ ใบรับรอง → CA → SPIFFE SAN เทียบกับ service identity → ลายเซ็น ก่อนขั้นตอน RBAC โดยหัวข้อที่เคลื่อนย้ายมูลค่า เช่น การ mint โทเคนพลังงาน ถูกบังคับให้ต้องมีซองที่ลงนามเสมอ

#heading(level: 2)[Adversary Model and Mitigations]
แบบจำลองผู้โจมตีพิจารณาผู้โจมตีที่สามารถสร้าง ดักจับ หรือส่งซ้ำข้อความบนเครือข่าย และอาจพยายามปลอมตัวตนของอุปกรณ์หรือบริการ แต่ไม่สามารถเข้าถึงกุญแจส่วนตัวที่จัดเก็บใน Vault หรือกุญแจประจำอุปกรณ์ได้ ภัยคุกคามที่พิจารณาและกลไกรองรับสรุปไว้ใน @tbl:threats

#figure(
  placement: top,
  scope: "parent",
  caption: [Threats considered and the mechanism that mitigates each.],
  text(size: 8pt)[
    #table(
    columns: (auto, 1fr, 1fr),
    align: (left + horizon, left + horizon, left + horizon),
    table.header([ภัยคุกคาม], [คำอธิบาย], [กลไกรองรับ]),
    [T1 ค่าอ่านปลอม/ส่งซ้ำ], [ปลอมหรือเล่นซ้ำค่าอ่านมาตรวัด], [ตรวจลายเซ็น Ed25519 ต่อค่าอ่าน (fail-closed) + order nullifier ในชั้น settlement],
    [T2 ปลอมตัวตนบริการ], [แอบอ้างเป็นบริการในเมช], [mTLS + SPIFFE identity → service role; ผู้ไม่ผ่านได้ `Unknown`],
    [T3 ปลอม publisher / mint], [ส่งคำสั่งเขียนเชนปลอมผ่าน NATS], [ซอง envelope ลงนามผูกกับใบรับรอง; หัวข้อ mint บังคับลงนาม],
    [T4 ส่งซ้ำคู่คำสั่งบนเชน], [ชำระคู่คำสั่งเดิมซ้ำ], [บัญชี order nullifier (PDA) — ชำระได้ครั้งเดียว],
    [T5 mint ซ้ำ/เกินสิทธิ์], [สร้างโทเคนเกินการผลิตจริง], [คีย์ idempotency `(meter_id, window)` + PDA + การร่วมลงนาม REC],
    )
  ],
) <tbl:threats>

#heading(level: 2)[Residual Trust and Out-of-Scope]
ระบบยังคงมีความเชื่อถือที่เหลืออยู่ซึ่งต้องระบุอย่างชัดเจน ประการแรก Aggregator Bridge และ governance/oracle authority เป็นผู้รับรองเงื่อนไขปริมาณพลังงานคงเหลือและ Grid stability ในชั้น off-chain โดยเงื่อนไขเหล่านี้ไม่ได้ถูกตรวจซ้ำบนเชน ดังนั้นผู้ใช้ต้องเชื่อถือความถูกต้องของชั้นตรวจสอบนอกเชนนี้ การประนีประนอม (compromise) หรือการสมรู้ร่วมคิด (collusion) ของ Aggregator Bridge หรือ oracle authority ยังไม่ถูกป้องกันด้วยกลไกเชิงรหัสวิทยาในต้นแบบปัจจุบัน แนวทางลด trust เพิ่มเติม เช่น multi-oracle attestation หรือ cryptographic proof เป็นงานในอนาคต (ดู @sec:discussion_limitations) ประการที่สอง ความปลอดภัยของชั้นฉันทามติตั้งอยู่บนสมมติฐานเครือข่าย permissioned แบบ PoA ที่ validator เป็นหน่วยงานได้รับอนุญาตและมีพฤติกรรมตามนโยบาย ซึ่งเป็นสมมติฐานเชิงสถาปัตยกรรมที่ยังไม่ได้ประเมินเชิงปริมาณ ประการที่สาม การยกเว้นตรวจสอบสำหรับโหมดพัฒนา (insecure mode และ plaintext fallback) ต้องถูกปิดในการใช้งานจริง มิฉะนั้นจะทำลายสมมติฐานความเชื่อถือข้างต้นทั้งหมด
